import { useEffect } from "react";

export function useGlobalHotkey() {
  useEffect(() => {
    // Global hotkeys handled by Tauri plugin on the Rust side
    // This hook is a placeholder for any JS-side hotkey handling
  }, []);
}
